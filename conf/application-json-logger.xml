<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="uk.gov.hmrc.play.logging.JsonEncoder"/>
    </appender>

    <logger name="uk.gov" level="${logger.uk.gov:-INFO}"/>

    <logger name="application" level="${logger.application:-INFO}"/>

    <logger name="v1.controllers" level="${logger.application:-INFO}"/>

    <logger name="v1.connectors" level="${logger.application:-INFO}"/>

    <logger name="v1.models.request" level="${logger.application:-INFO}"/>

    <logger name="v1.services" level="${logger.application:-INFO}"/>

    <logger name="v1.utils" level="${logger.application:-INFO}"/>

    <logger name="utils" level="${logger.application:-INFO}"/>

    <logger name="config" level="${logger.application:-INFO}"/>

    <root level="${logger.application:-INFO}">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
